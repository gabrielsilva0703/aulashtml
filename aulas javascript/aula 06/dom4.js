let elemento1 = document.getElementsByClassName("elemento1")
elemento1.style.backgroundColor = "blue"

for (let contador = 2; contador < elemento1.length; contador++) {
    elementos[contador].style.backgroundColor = "red" ;
    
}